# init:
#   - "./create.sh"
merge:
  - "merge %garden our %base"
  - "merge %groups our %base"
mount:
  - "mount %garden"
  - "mount %groups"
sync:
  - "./sync.sh"
commit:
  - "commit %garden"
  - "commit %groups"
install:
  - "install our %garden"
  - "install our %groups"
